using UnityEngine;

public class IMPORTANTNOTE : MonoBehaviour
{

    [Tooltip("This is an important note for the Garage Scripts. Add any specific instructions here.")]
    public string note = "Please ikke override disse 3 prefabs <3";

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
